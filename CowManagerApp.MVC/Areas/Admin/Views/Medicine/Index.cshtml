<link rel="stylesheet" href="~/css/Medicine.css" asp-append-version="true" />
@model IEnumerable<CowManager.Models.Models.Medicine>
@{
    ViewData["Title"] = "List";
}
<h1>@ViewData["Title"]</h1>
<a asp-action="MedicineCreate">Create</a>

<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Name
            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Name
                </td>

                <td>
                    <a asp-action="MedicineDetails" asp-route-id="@item.Id">Details</a>|
                    <a asp-action="MedicineEdit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="MedicineDelete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>

        }
    </tbody>
</table>
