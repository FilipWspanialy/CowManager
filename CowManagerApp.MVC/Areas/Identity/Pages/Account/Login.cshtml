@page
@model LoginModel
<link rel="stylesheet" href="~/css/Create.css" asp-append-version="true" />
@{
    ViewData["Title"] = "Log in";
}

<h1 class="text-center" style="color: black;">@ViewData["Title"]</h1>
<div class="row justify-content-center mt-5">
    <div class="col-md-6 col-lg-4">
        <section class="card p-4 shadow-sm">
            <form id="account" method="post">
                <h2 class="text-center mb-4">Use a local account to log in</h2>
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="form-label">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="form-label">Password</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-check mb-3">
                    <input class="form-check-input" asp-for="Input.RememberMe" />
                    <label asp-for="Input.RememberMe" class="form-check-label">
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="btn btn-primary w-100 rounded-pill shadow-sm">Log in</button>
                </div>
                <div class="text-center mt-3">
                    <div class="text-center mt-3">
    <button type="button" class="btn btn-secondary w-100" onclick="location.href='@Url.Page("./Register", new { returnUrl = Model.ReturnUrl })'">
        Register as a new user
    </button>
</div>
                </div>
            </form>
        </section>
    </div>
</div>

@section Scripts {
        <partial name="_ValidationScriptsPartial" />
}